# Story 4.0 — DataTable Avançado: Sorting, Filtering, Seleção, Export CSV

> **Status:** Draft
> **Épico:** 06 — DataTable Avançado · **Marco:** M6.0
> **Preparada por:** `@sm` (River) · **Data:** 2026-06-22
> **Insumos:** Story 0.4 ✅ (DataTable base) · Story 3.0 ✅ (overflow-x-auto + sticky thead) · `docs/prd/epico-06-datatable-avancado.md` · `knowledge-base/negocio.md`
> **Bloqueante para:** Estética IA M1 (listas interativas), AIOX Gestão (tabelas financeiras), ML Lab (dataset tables), AIOX Auth (user tables)
> **Depende de:** Story 0.4 ✅ (DataTable) · Story 3.0 ✅ (scroll + sticky)

---

## Story

**Como** desenvolvedor de um módulo consumidor do ecossistema,
**quero** um `DataTable` com sorting por coluna, filtros por coluna, seleção de linhas com checkbox, export CSV e empty state com CTA,
**para que** eu possa construir listas de produção (clientes, agendamentos, transações, datasets) sem reimplementar lógica de tabela em cada módulo.

---

## Contexto

O `DataTable` atual (Story 0.4) tem paginação e loading skeleton — suficiente para protótipos, mas bloqueante para uso real. Estética IA precisa ordenar clientes por nome/data, filtrar agendamentos por status, e selecionar múltiplos para ações em lote. AIOX Gestão precisa exportar relatórios financeiros. ML Lab precisa selecionar linhas de dataset para operações.

Sem essas features no design system, cada módulo implementa a própria versão — divergência visual garantida.

**Decisão arquitetural:** Toda a lógica de sorting, filtering e seleção é client-side sobre o array `data` completo. Server-side pagination/sorting é responsabilidade dos módulos consumidores — o componente gerencia apenas o estado local. Limite documentado: ~500 linhas sem virtualização.

---

## Critérios de aceitação

### 1. Estrutura de arquivos

```
src/
└── organisms/
    └── DataTable/
        ├── DataTable.tsx          # Atualizar — sorting + filtering + seleção + export + empty action + actions column
        ├── DataTable.stories.tsx  # Atualizar — stories para cada feature nova
        └── DataTable.test.tsx     # Atualizar — 10+ novos testes
```

**Nota:** `src/index.ts` — sem alteração. `DataTable`, `DataTableProps`, `DataTableColumn` já exportados.

---

### 2. Interface atualizada (zero breaking change — todos os novos props são opcionais)

```ts
// Atualização em DataTableColumn<T>
interface DataTableColumn<T> {
  key: keyof T | string
  header: string
  render?: (value: unknown, row: T) => React.ReactNode
  width?: string
  // NOVOS props — todos opcionais:
  sortable?: boolean                               // padrão: false
  filterable?: boolean                             // padrão: false
  filterType?: 'text' | 'select'                  // padrão: 'text'
  filterOptions?: { value: string; label: string }[] // apenas quando filterType='select'
}

// Atualização em DataTableProps<T>
interface DataTableProps<T extends object> {
  columns: DataTableColumn<T>[]
  data: T[]
  pageSize?: number
  emptyMessage?: string
  loading?: boolean
  className?: string
  // NOVOS props — todos opcionais:
  selectable?: boolean                             // padrão: false
  onSelectionChange?: (selected: T[]) => void
  exportable?: boolean                             // padrão: false
  exportFilename?: string                          // padrão: 'export'
  emptyAction?: React.ReactNode
  actionsColumn?: (row: T) => React.ReactNode
}
```

---

### 3. Sorting por coluna

#### 3a. Estado interno

```ts
type SortDirection = 'asc' | 'desc' | null
const [sortKey, setSortKey] = useState<string | null>(null)
const [sortDir, setSortDir] = useState<SortDirection>(null)
```

#### 3b. Ciclo de clique no header

| Clique | Estado anterior | Estado novo |
|--------|----------------|-------------|
| 1º | null | ASC |
| 2º | ASC | DESC |
| 3º | DESC | null (ordem original) |
| Clique em outra coluna | qualquer | ASC na nova coluna |

#### 3c. Ícones no header (usar `Icon` do design system)

| Estado | Ícone lucide |
|--------|-------------|
| Neutro (sortable, não ativo) | `ArrowUpDown` |
| ASC ativo | `ArrowUp` |
| DESC ativo | `ArrowDown` |

```tsx
{col.sortable && (
  <button
    onClick={() => handleSort(String(col.key))}
    className="inline-flex items-center gap-1 hover:text-neutral-900"
    aria-label={`Ordenar por ${col.header}`}
  >
    {col.header}
    <Icon
      name={sortKey === String(col.key) ? (sortDir === 'asc' ? 'ArrowUp' : 'ArrowDown') : 'ArrowUpDown'}
      size={14}
      className={sortKey === String(col.key) ? 'text-primary-600' : 'text-neutral-400'}
    />
  </button>
)}
```

#### 3d. Lógica de sort

```ts
function sortData(rows: T[]): T[] {
  if (!sortKey || !sortDir) return rows
  return [...rows].sort((a, b) => {
    const aVal = (a as Record<string, unknown>)[sortKey]
    const bVal = (b as Record<string, unknown>)[sortKey]
    if (aVal == null) return 1
    if (bVal == null) return -1
    const cmp = String(aVal).localeCompare(String(bVal), 'pt-BR', { numeric: true, sensitivity: 'base' })
    return sortDir === 'asc' ? cmp : -cmp
  })
}
```

- Sorting opera sobre o `data` **completo** antes de filtrar e paginar
- `localeCompare` com `pt-BR` + `numeric: true` ordena "Item 10" após "Item 9"
- Valores `null`/`undefined` vão para o final independente da direção

---

### 4. Filtering por coluna

#### 4a. Estado interno

```ts
const [filters, setFilters] = useState<Record<string, string>>({})
// chave = String(col.key), valor = string do filtro ativo
```

#### 4b. Input de filtro de texto (quando `filterable=true` e `filterType='text'`)

```tsx
{col.filterable && (
  <div className="mt-1">
    <input
      type="text"
      placeholder={`Filtrar ${col.header}...`}
      value={filters[String(col.key)] ?? ''}
      onChange={e => handleFilter(String(col.key), e.target.value)}
      className="w-full px-2 py-1 text-xs border border-neutral-200 rounded focus:outline-none focus:ring-1 focus:ring-primary-500"
      aria-label={`Filtrar por ${col.header}`}
    />
  </div>
)}
```

#### 4c. Select de filtro (quando `filterType='select'`)

```tsx
{col.filterable && col.filterType === 'select' && (
  <select
    value={filters[String(col.key)] ?? ''}
    onChange={e => handleFilter(String(col.key), e.target.value)}
    className="w-full mt-1 px-2 py-1 text-xs border border-neutral-200 rounded focus:outline-none focus:ring-1 focus:ring-primary-500"
    aria-label={`Filtrar por ${col.header}`}
  >
    <option value="">Todos</option>
    {col.filterOptions?.map(opt => (
      <option key={opt.value} value={opt.value}>{opt.label}</option>
    ))}
  </select>
)}
```

#### 4d. Indicador de filtros ativos

Acima da tabela, quando `activeFilterCount > 0`:

```tsx
{activeFilterCount > 0 && (
  <div className="flex items-center gap-2 mb-2">
    <Badge variant="secondary">{activeFilterCount} {activeFilterCount === 1 ? 'filtro ativo' : 'filtros ativos'}</Badge>
    <button
      onClick={() => setFilters({})}
      className="text-xs text-primary-600 hover:underline"
    >
      Limpar filtros
    </button>
  </div>
)}
```

#### 4e. Lógica de filtering

```ts
function filterData(rows: T[]): T[] {
  return rows.filter(row =>
    Object.entries(filters).every(([key, val]) => {
      if (!val) return true
      const cellVal = String((row as Record<string, unknown>)[key] ?? '')
      return cellVal.toLowerCase().includes(val.toLowerCase())
    })
  )
}
```

**Pipeline de dados:** `data` → `filterData` → `sortData` → `paginação` → `renderização`

---

### 5. Seleção de linhas

#### 5a. Estado interno

```ts
const [selected, setSelected] = useState<Set<number>>(new Set())
// índice no array `data` original (não na página atual)
```

#### 5b. Checkbox no header (seleciona/deseleciona toda a página atual)

```tsx
{selectable && (
  <th scope="col" className="w-10 px-4 py-3">
    <input
      type="checkbox"
      checked={pageSelected && slicedIndexes.length > 0}
      ref={el => { if (el) el.indeterminate = partialSelected }}
      onChange={e => handleSelectPage(e.target.checked)}
      aria-label="Selecionar todos da página"
      className="rounded border-neutral-300 text-primary-600 focus:ring-primary-500"
    />
  </th>
)}
```

- `pageSelected`: todos os índices da página atual estão em `selected`
- `partialSelected`: alguns (mas não todos) estão em `selected` → estado `indeterminate`

#### 5c. Checkbox por linha

```tsx
{selectable && (
  <td className="w-10 px-4 py-3">
    <input
      type="checkbox"
      checked={selected.has(originalIndex)}
      onChange={e => handleSelectRow(originalIndex, e.target.checked)}
      aria-label={`Selecionar linha ${originalIndex + 1}`}
      className="rounded border-neutral-300 text-primary-600 focus:ring-primary-500"
    />
  </td>
)}
```

#### 5d. Estilo da linha selecionada

```tsx
<tr className={cn('hover:bg-neutral-50', selected.has(originalIndex) && 'bg-primary-50')}>
```

#### 5e. Contador de selecionados

Acima da tabela quando `selected.size > 0`:

```tsx
{selectable && selected.size > 0 && (
  <div className="flex items-center gap-2 mb-2 text-sm text-neutral-600">
    <span>{selected.size} {selected.size === 1 ? 'selecionado' : 'selecionados'}</span>
  </div>
)}
```

#### 5f. `onSelectionChange`

Chamado sempre que `selected` muda:

```ts
useEffect(() => {
  if (onSelectionChange) {
    onSelectionChange(Array.from(selected).map(i => data[i]).filter((r): r is T => r !== undefined))
  }
}, [selected, data, onSelectionChange])
```

---

### 6. Export CSV

#### 6a. Botão

Renderizado no canto superior direito do componente quando `exportable=true`:

```tsx
<button
  onClick={handleExport}
  className="flex items-center gap-1 px-3 py-1.5 text-sm border border-neutral-200 rounded-md hover:bg-neutral-50 text-neutral-700"
>
  <Icon name="Download" size={14} />
  Exportar CSV
</button>
```

#### 6b. Lógica de export

```ts
function handleExport() {
  // Dados a exportar: selecionados > filtrados > todos
  const rows = selected.size > 0
    ? Array.from(selected).map(i => data[i]).filter((r): r is T => r !== undefined)
    : filterData(data)

  const headers = columns
    .filter(col => col.key !== '__actions__')
    .map(col => col.header)

  const csvRows = rows.map(row =>
    columns
      .filter(col => col.key !== '__actions__')
      .map(col => {
        const val = (row as Record<string, unknown>)[String(col.key)]
        const str = String(val ?? '')
        // Escapar aspas duplas e envolver em aspas se necessário
        return str.includes(',') || str.includes('"') || str.includes('\n')
          ? `"${str.replace(/"/g, '""')}"`
          : str
      })
      .join(',')
  )

  // UTF-8 BOM para Excel PT-BR reconhecer acentos
  const bom = '﻿'
  const csv = bom + [headers.join(','), ...csvRows].join('\r\n')
  const blob = new Blob([csv], { type: 'text/csv;charset=utf-8;' })
  const url = URL.createObjectURL(blob)
  const link = document.createElement('a')
  link.href = url
  link.download = `${exportFilename ?? 'export'}.csv`
  link.click()
  URL.revokeObjectURL(url)
}
```

---

### 7. Empty state com ação

Quando `data.length === 0` e `loading=false` (após aplicar filtros):

```tsx
<tr>
  <td colSpan={totalColumns} className="px-4 py-12">
    <div className="flex flex-col items-center justify-center text-center">
      <Icon name="Inbox" size={40} className="text-neutral-300 mb-3" />
      <p className="text-sm text-neutral-500">{emptyMessage}</p>
      {emptyAction && <div className="mt-4">{emptyAction}</div>}
    </div>
  </td>
</tr>
```

`totalColumns` = `columns.length` + (selectable ? 1 : 0) + (actionsColumn ? 1 : 0)

---

### 8. Coluna de ações

Quando `actionsColumn` é fornecido, renderiza uma última coluna sem header:

```tsx
{actionsColumn && (
  <th scope="col" className="px-4 py-3 text-right">
    <span className="sr-only">Ações</span>
  </th>
)}

// Em cada linha:
{actionsColumn && (
  <td className="px-4 py-3 text-right">
    {actionsColumn(row)}
  </td>
)}
```

---

### 9. Stories (Storybook)

Adicionar às stories existentes:

- `Sortable` — 3 colunas com `sortable=true`, dados mistos (string + número)
- `Filterable` — 1 coluna `filterType='text'` + 1 `filterType='select'` com `filterOptions`
- `Selectable` — `selectable=true` com `onSelectionChange` logado no Actions
- `Exportable` — `exportable=true` com `exportFilename="clientes"`
- `WithEmptyAction` — `data=[]` com `emptyAction={<Button>+ Novo Cliente</Button>}`
- `WithActionsColumn` — `actionsColumn` com botões Editar e Excluir por linha
- `FullFeatured` — todas as features ativas juntas com 20 linhas de dados realistas

---

### 10. Testes (mínimo 10 novos — além dos 5 existentes)

```
// Sorting
- [ ] Clicar header sortable ordena ASC (primeiro item esperado)
- [ ] Segundo clique ordena DESC
- [ ] Terceiro clique restaura ordem original
- [ ] Ícone ArrowUp visível quando ASC ativo

// Filtering
- [ ] Digitar no filtro de texto reduz as linhas visíveis
- [ ] filterType='select' com opção selecionada filtra corretamente
- [ ] Botão "Limpar filtros" remove todos os filtros e restaura dados
- [ ] Badge "N filtros ativos" aparece quando há filtro ativo

// Seleção
- [ ] Checkbox de linha chama onSelectionChange com a linha selecionada
- [ ] Checkbox do header seleciona todas as linhas da página atual
- [ ] Linha selecionada tem classe bg-primary-50
- [ ] Contador "N selecionados" aparece quando há seleção

// Export
- [ ] handleExport gera Blob com headers corretos (mock URL.createObjectURL)

// Empty state
- [ ] emptyAction renderiza CTA dentro do empty state
- [ ] Ícone Inbox presente no empty state

// Regressão
- [ ] Props existentes (columns, data, pageSize, emptyMessage, loading) funcionam sem novos props
```

---

## Escopo negativo

- ❌ Virtualização de lista — máx. ~500 linhas sem virtualização; Épico 07 se necessário
- ❌ Edição inline de células — responsabilidade dos módulos
- ❌ Colunas redimensionáveis via drag
- ❌ Agrupamento de linhas (row grouping)
- ❌ Multi-column sort — apenas uma coluna ordenada por vez
- ❌ Server-side pagination/sorting — client-side apenas
- ❌ Export para Excel (.xlsx) — CSV apenas
- ❌ Print CSS — fora de escopo
- ❌ Dark mode — fora de escopo global
- ❌ Animações com `framer-motion`

---

## Notas técnicas

### Pipeline de dados (ordem obrigatória)

```
data (prop) → filterData() → sortData() → sliceForPage() → render
```

Filtering antes de sorting garante que o sort opere apenas nos dados visíveis. Paginação no final garante que "página 1 de N" reflita o filtro aplicado. Resetar para página 1 sempre que `filters` ou `sortKey` mudar.

### Seleção por índice original (não por página)

`selected` armazena índices do array `data` **original** (antes de filter/sort/paginate). Isso garante que seleções feitas na página 1 persistam ao navegar para página 2 e voltar. A conversão para linha real é `data[i]`.

### Export CSV — `URL.createObjectURL` em testes

Em ambiente Vitest/JSDOM, `URL.createObjectURL` não existe. Mockar antes dos testes:

```ts
beforeAll(() => {
  global.URL.createObjectURL = vi.fn(() => 'blob:mock')
  global.URL.revokeObjectURL = vi.fn()
})
```

### `indeterminate` via ref

O estado `indeterminate` do checkbox não é uma prop React — requer acesso direto ao DOM via `ref`:

```tsx
const headerCheckRef = useRef<HTMLInputElement>(null)
useEffect(() => {
  if (headerCheckRef.current) {
    headerCheckRef.current.indeterminate = partialSelected
  }
}, [partialSelected])
```

### `localeCompare` para PT-BR

```ts
String(aVal).localeCompare(String(bVal), 'pt-BR', { numeric: true, sensitivity: 'base' })
```

- `numeric: true` — "Item 10" ordena após "Item 9" (não "Item 1" → "Item 10" → "Item 9")
- `sensitivity: 'base'` — ignora maiúsculas/minúsculas e acentos na ordenação

### Filtering sobre valor bruto (não render)

O `render` pode retornar JSX — comparar `String(JSX)` sempre retorna `[object Object]`. Filtrar sempre sobre o valor bruto: `(row as Record<string, unknown>)[key]`.

---

## Complexidade

**Estimativa:** L (Large)
- 6 features novas em um único componente com estado complexo interconectado
- Pipeline de dados (filter → sort → paginate) requer atenção à ordem e reset de página
- `indeterminate` via ref é um caso especial do React
- Export CSV com UTF-8 BOM e mock em testes
- 10+ testes novos além dos 5 existentes
- Estimativa: 12-18 horas de desenvolvimento

---

## Valor de negócio

Desbloqueia todos os módulos consumidores para construir listas interativas de produção. Estética IA pode lançar M1 (piloto de validação) com lista de clientes real. AIOX Gestão pode gerar relatórios exportáveis. ML Lab pode operar sobre datasets com seleção em lote. Elimina divergência visual entre módulos — qualquer tabela do ecossistema usa o mesmo componente.

---

## CodeRabbit Integration

Foco de revisão automatizada:
- Pipeline de dados: `filterData → sortData → sliceForPage` na ordem correta
- Reset de `page` para 1 quando `filters` ou `sortKey` mudam
- `indeterminate` do checkbox de header via `ref` (não via prop React)
- `useEffect` para `onSelectionChange` com dependências corretas (sem loop infinito)
- Export CSV: UTF-8 BOM (`﻿`) presente; `URL.createObjectURL` chamado; link removido após click
- Seleção por índice do `data` original (não da página atual)
- `totalColumns` inclui coluna de seleção e ações no `colSpan` do empty state
- Zero `any`, zero `!` non-null — DTS build limpo
- Todos os 5 testes originais do DataTable ainda passando (regressão)

---

## Quality Gate

- `@dev` (Dex): implementa as 6 features; valida pipeline filter→sort→paginate manualmente com dados reais; valida export CSV abrindo no Excel PT-BR (ou verificando BOM no blob); roda `npm test -- --run` para 184+ testes passando; `npm run build` sem erros DTS
- `@qa` (Quinn): valida sorting com strings acentuadas PT-BR; confirma que seleção persiste ao mudar de página; valida `indeterminate` no checkbox do header (selecionar alguns mas não todos); verifica axe DevTools no DataTable completo; confirma regressão nos 5 testes originais
- `@devops` (Gage): push + verificar build CI

---

## File List

| Arquivo | Ação | Agente |
|---------|------|--------|
| `src/organisms/DataTable/DataTable.tsx` | Atualizar — 6 features novas, zero breaking change | @dev (Dex) |
| `src/organisms/DataTable/DataTable.stories.tsx` | Atualizar — 7 stories novas | @dev (Dex) |
| `src/organisms/DataTable/DataTable.test.tsx` | Atualizar — 10+ testes novos | @dev (Dex) |

---

## Riscos

| Risco | Probabilidade | Mitigação |
|-------|--------------|-----------|
| `URL.createObjectURL` sem mock → teste falha em JSDOM | Alto | `beforeAll` com `vi.fn()` — documentado em notas técnicas |
| Export CSV com acentos corrompidos no Excel | Alto | UTF-8 BOM obrigatório (`﻿`) — verificar no teste do blob |
| Loop infinito no `useEffect` do `onSelectionChange` | Médio | `selected` no dep array como Set — usar `Array.from(selected).join(',')` como dep estável se necessário |
| Sort com `localeCompare` diferente entre Node e browser | Baixo | Testar com strings PT-BR acentuadas (ex: "Ângela" < "Bruno") |
| Reset de página ao filtrar — usuário perde contexto | Baixo | Comportamento esperado e documentado; paginação sempre começa em 1 após filter/sort |

---

## Dev Agent Record

### Agent Model Used
_a preencher_

### Completion Notes
_a preencher_

### File List
_a preencher após implementação_

---

## Change Log

| Data | Agente | Ação |
|------|--------|------|
| 2026-06-22 | @sm (River) | Story criada — DataTable Avançado, Épico 06 |
